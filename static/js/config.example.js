/**
 * ============================================================
 * CONFIG EXAMPLE - Supabase Configuration
 * ============================================================
 * 1. Copy this file as "config.js"
 * 2. Replace placeholders with your Supabase credentials
 * 3. Never commit config.js (already in .gitignore)
 * 
 * Get credentials: supabase.com/dashboard → Settings → API
 * ============================================================
 */

export const CONFIG = {
    SUPABASE_URL: 'https://YOUR-PROJECT.supabase.co',
    SUPABASE_ANON_KEY: 'YOUR-ANON-KEY-HERE',
    API_BASE_URL: window.location.origin
};
